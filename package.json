{
  "private": true,
  "scripts": {
    "build": "purs-tidy check --config-require src && node_modules/.bin/spago -x spago.dhall build --purs-args \"--censor-lib --strict\"",
    "dev": "node_modules/.bin/spago -x spago.dhall build -w --purs-args \"--censor-lib\"",
    "test": "node_modules/.bin/spago test"
  },
  "devDependencies": {
    "spago": "^0.20.9",
    "purescript-psa": "^0.8.2",
    "purs-tidy": "^0.9.2"
  }
}
